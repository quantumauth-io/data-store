# Foundry (Anvil) Docker assets

This folder contains docker-related configuration for running a local Anvil chain
for smart contract tests/benchmarks (PQ signature gas benchmarking).

Next steps:
- add an `anvil` service to docker-compose
- optionally add a `forge` runner service
- add a `contracts/` folder (Foundry project) later